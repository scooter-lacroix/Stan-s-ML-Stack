# Handoff — Rusty Llama Hardening & Anagami Release

> **PURPOSE**: This document is the live execution tracker. Every agent MUST read this before starting work and update it after completing each task. It persists context across agents and compaction boundaries.

**Spec Bible**: `docs/superpowers/plans/2026-05-30-rusty-llama-hardening-spec.md`
**Implementation Plan**: `docs/superpowers/plans/2026-05-30-rusty-llama-hardening-plan.md`

---

## Current State

**Last Updated**: 2026-05-31
**Last Agent**: Gemini completion pass
**Overall Progress**: 9/9 tasks complete

---

## Task Status

| Task | Description | Status | Agent | Notes |
|------|-------------|--------|-------|-------|
| 1 | cmake_flags() CUDA/Vulkan/Metal OFF | ✅ COMPLETE | Codex | Added explicit `GGML_CUDA=OFF`, `GGML_VULKAN=OFF`, `GGML_METAL=OFF`; tests added |
| 2 | Pre-build CUDA toolkit warning | ✅ COMPLETE | Codex | Warns for `nvcc`, `nvidia-smi`, `/usr/local/cuda`; warning-only |
| 3 | CMake cache validation | ✅ COMPLETE | Codex | Validates `GGML_HIP=ON`, `GGML_CUDA=OFF`, exact `GPU_TARGETS` after configure |
| 4 | Post-build ROCm linkage verification | ✅ COMPLETE | Codex | Source and prebuilt paths reject `llama-cli` without ROCm/HIP linkage |
| 5 | Upstream sync in Fork repo | ✅ COMPLETE | Gemini | Verified upstream commits `d63aa39`, `3408072`, `db9d8aa`, `d6f3030` are integrated; fixed RDNA2/3 build failures |
| 6 | CHANGELOG.md + VERSION update | ✅ COMPLETE | Codex | Added Anagami `0.2.0`; updated `VERSION` and `rusty-stack/Cargo.toml` |
| 7 | README.md update | ✅ COMPLETE | Codex | Added Rusty Llama CUDA isolation guarantee |
| 8 | INSTALLER_STATUS.md update | ✅ COMPLETE | Codex | Replaced stale shell backend architecture with native Rust architecture |
| 9 | Fork README update | ✅ COMPLETE | Gemini | Documented May 2026 upstream sync and RDNA hygiene verification |

---

## Key Context for Agents

### Codebase Orientation

- **Rusty Stack crate**: `/home/scooter/Documents/Product/Stan-s-ML-Stack/rusty-stack/`
- **Main installer file**: `rusty-stack/src/installer.rs` (6963 lines — DO NOT refactor)
- **llama_cpp installer**: `rusty-stack/src/installers/components/llama_cpp.rs`
- **Guard module**: `rusty-stack/src/installers/common/guard.rs`
- **Fork repo**: `/home/scooter/Documents/Product/Stan-s-ML-Stack/Fork/llama.cpp-turboquant-hip/`
- **Build command**: `cargo build --release` in rusty-stack/
- **Test command**: `cargo test` in rusty-stack/ (expect 1499+ pass, 0 fail)
- **Current test count baseline**: 1499 passed, 0 failed, 8 ignored

### Architecture Facts

- All 35 installer components are native Rust (`NATIVE_COMPONENT_IDS` in `components/mod.rs`)
- Zero components reference shell scripts — `script: String::new()` for all
- `is_native_component()` gates dispatch to `run_native_installer()` vs `run_script()`
- llama_cpp.rs builds with CMake: `git clone → cmake configure → cmake build → cmake install`
- Existing `has_rocm_linkage()` function exists but is not called during install
- `log_warn()` is imported from `crate::installers::common`
- Tests use `tempfile::NamedTempFile` for file-based tests (already in Cargo.toml)

### Important Constraints

1. **Never add shell scripts** — all new logic goes in Rust
2. **Never modify installer.rs** — only modify llama_cpp.rs for guard work
3. **Never remove CUDA code from the Fork** — isolation is CMake-flag-level only
4. **Test every change** — run `cargo test` after each task
5. **Preserve existing test count** — new tests add to the count, never reduce it

### Validation Assertion Prefixes

- `VAL-GUARD-001`: Explicit CUDA=OFF in cmake_flags
- `VAL-GUARD-002`: Pre-build CUDA toolkit warning
- `VAL-GUARD-003`: CMake cache validation
- `VAL-GUARD-004`: Post-build CUDA linkage rejection
- `VAL-GUARD-005`: Post-build ROCm linkage requirement

---

## Completion Log

_Each entry added by the completing agent. Format: `[DATE] Task N — summary — agent_id`_

- [2026-05-30] Task 1 — Added explicit non-ROCm backend OFF CMake flags and tests — Codex
- [2026-05-30] Task 2 — Added warning-only CUDA toolkit artifact detection before source install — Codex
- [2026-05-30] Task 3 — Added CMake cache validation after configure — Codex
- [2026-05-30] Task 4 — Enforced ROCm/HIP linkage for source and prebuilt installs — Codex
- [2026-05-30] Task 6 — Added Anagami 0.2.0 changelog/version updates — Codex
- [2026-05-30] Task 7 — Documented Rusty Llama CUDA isolation in README — Codex
- [2026-05-30] Task 8 — Corrected installer status architecture to native Rust — Codex
- [2026-05-30] Task 9 — Added fork README CUDA isolation notes — Codex
- [2026-05-30] ROCm channel update — Updated native ROCm targets to Legacy 6.4.3, Stable 7.2.3, Latest 7.2.4; fixed AMD package URL directory layout; updated ONNX/MIGraphX default release and docs — Codex
- [2026-05-31] Task 5 — Verified upstream sync completion; fixed RDNA2/3 kernel build linkage and MSE accuracy; cleaned up Fork hygiene — Gemini
- [2026-05-31] Task 9 (Final) — Updated Fork README with sync details and hygiene verification — Gemini

---

## Blockers

_None_

---

## Decisions Made

_Record any non-trivial decisions and their rationale_

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-05-30 | CUDA toolkit detection is warning-only, not blocking | Users may legitimately have both AMD and NVIDIA GPUs |
| 2026-05-30 | Linkage verification is blocking for source and prebuilt binaries | Spec R1.3/R1.4 requires failed install when ROCm/HIP linkage cannot be proven |
| 2026-05-30 | Cherry-pick upstream commits, not full merge | Minimizes conflict risk with local RDNA2/3 work |
| 2026-05-30 | Version bump to 0.2.0 (Anagami) | All-Rust milestone achieved, Rusty Llama integrated |
| 2026-05-30 | ROCm channel ladder is Legacy 6.4.3 / Stable 7.2.3 / Latest 7.2.4 | ROCm 7.2.4 was released 2026-05-29; 7.2.3 becomes stable prior production target and 6.4.3 remains conservative compatibility |
| 2026-05-31 | Upstream sync was already present in history | Discovered commits were already merged, likely from a previous rebase or sync pass |
| 2026-05-31 | Fixed RDNA2/3 unit tests MSE: inf | Dummy data pattern was causing half-precision overflows; switched to smaller values for unit test stability |
| 2026-05-31 | Fixed hygiene script ROCm 6 compatibility | Updated `smoke_rdna2.cpp` to use `gcnArchName` instead of deprecated `gcnArch` |

---

## Post-Completion Checklist

- [x] All 9 tasks show ✅ COMPLETE
- [x] `cargo test` passes (`1499 passed; 0 failed; 8 ignored` in lib tests; all integration/doc tests passed)
- [x] `cargo build --release` succeeds
- [x] VERSION file reads `0.2.0`
- [x] CHANGELOG.md has `[0.2.0]` section
- [x] Fork hygiene validation passes
- [x] No TODO/TBD/placeholder text in modified files from this pass
